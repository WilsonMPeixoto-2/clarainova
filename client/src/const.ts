export { COOKIE_NAME, ONE_YEAR_MS } from "@shared/const";

/**
 * Stub function - not used in public-only application
 * Kept for compatibility with existing code that may reference it
 */
export const getLoginUrl = () => {
  return "/";
};
